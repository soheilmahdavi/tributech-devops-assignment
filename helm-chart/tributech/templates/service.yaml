apiVersion: v1
kind: Service
metadata:
  name: {{ include "tributech.fullname" . }}
  labels:
    {{- include "tributech.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tributech.selectorLabels" . | nindent 4 }}
